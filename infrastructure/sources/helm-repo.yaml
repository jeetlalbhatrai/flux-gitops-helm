apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: helm-charts-repo
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/jeetlalbhatrai/flux-gitops-helmcharts
  ref:
    branch: main